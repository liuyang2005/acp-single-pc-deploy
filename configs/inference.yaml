network:
  bind_endpoint: tcp://127.0.0.1:5555

model:
  acp_root: ../adaptive_compliance_policy
  device: cuda:0
  action_period_s: 0.5035
  inference_seed: 42
  expected_camera_view: wrist
  minimum_checkpoint_epoch: 700

logging:
  root: logs/inference
